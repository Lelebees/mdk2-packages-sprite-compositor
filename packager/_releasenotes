- 1.0.12
  - CRITICAL FIX: Changed contentFiles buildAction from "Compile" to "None" to prevent duplicate includes when .projitems is imported
- 1.0.11
  - CRITICAL FIX: Changed .targets file to properly import .projitems instead of manually including .cs files (was attempting to compile .projitems as source)
- 1.0.10
  - CRITICAL FIX: Removed invalid packageType that prevented package installation
- 1.0.9
  - Auto-generation of project URLs for library packages based on git repository and library location
  - Fixed packager tool's own repository URLs to point to malforge/mdk2-packages
- 1.0.8
  - Previous version (no documented changes)
